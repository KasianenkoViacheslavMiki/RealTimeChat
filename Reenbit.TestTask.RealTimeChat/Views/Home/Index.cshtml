@model IEnumerable<Reenbit.TestTask.RealTimeChat.Models.Message>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<html>
<h1>Private chat</h1>
<div class="col-12 col-lg-7 col-xl-9">
                    <div class="py-2 px-4 border-bottom d-none d-lg-block">
                        <div class="d-flex align-items-center py-1">
                            <div class="flex-grow-1 pl-3">
                                <strong>NameWhomWrite</strong>
                                <div class="text-muted small"><em>Typing...</em></div>
                            </div>
                        </div>
</div>
@*<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TextMessage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateMessage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Room)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>*@
        <div id="chat" class="chat-messages p-4" style="overflow-y: scroll; height: 570px;" >
@foreach (var item in Model) {
    
    
        <div class="chat-message-left pb-4">
                     <div class="flex-shrink-1 bg-light rounded py-2 px-3 ml-3">
                        <div class="font-weight-bold mb-1">@Html.DisplayFor(modelItem => item.User.UserName)</div>
                        <div>@Html.DisplayFor(modelItem => item.TextMessage) </div>
                    </div>
        </div>
    
    
    @*
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TextMessage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateMessage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Room.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
        </tr>*@
}
@*    </tbody>
</table>*@
</div>
<script>
    document.querySelector('#chat').lastElementChild.scrollIntoView();

</script>
<div class="flex-grow-0 py-3 px-4 border-top">
    <div class="input-group">
        <input id="messageInput"type="text" class="form-control" placeholder="Type your message">
        <button id="sendButton" class="btn btn-primary" value="Send Message">Send</button>
    </div>
</div>

</html>